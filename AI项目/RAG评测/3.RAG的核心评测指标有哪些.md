# ✅RAG的核心评测指标有哪些？

## 本文脑图

```markmap
- RAG核心评测指标
  - 检索层（Context Relevance 是目标，指标是尺子）
    - Context Recall 查全率
    - Context Precision 查准率
    - F1 = 2PR/(P+R) 双高才高
    - MRR/NDCG 考核排名靠前
    - 实现方式对比
      - 传统P/R：死板统计 需标注GT
      - Context P/R：语义评估 LLM-as-Judge 免GT
  - 生成层
    - Faithfulness 是否严格基于上下文 低分=幻觉 免GT
    - Answer Relevancy 直接回答原问题 免GT
    - Answer Correctness 金标准 必须GT
      - 语义相似度+事实准确度加权
      - 事实权重更高 防"正确的废话"
  - 端到端与工程化
    - A/B测试 黄金测试集
    - 鲁棒性：抗噪声攻击 无害内容
    - 成本：检索延迟 QPS API调用费
```


##### ✅RAG测评之RAG Triad

前面说过了，RAG系统中三个要素，分别是问题、答案以及检索资料。所以，我们需要有一套方法论来针对问题和答案、问题和检索资料、检索资料和答案之间的组合做全面评估。 RAG Triad（RAG 三元组）是一套专门用于评估 RAG（检索增强生成）

LLMentor

通过前面的课程，了解了RAG Triad之后，我们知道， RAG Triad 是一个**评估体系或方法论。**那么这里面定义的评估指标应该具体如何度量呢？


我们都知道，一个RAG系统，主要干两件事，第一件事儿是检索（**Retrieval**）、第二件事是生成（**Generation**）。所以才叫Retrieval-Augmented Generation


那么，在评测上，通常也是针对两个阶段都有各自的评估维度，同时也会结合端到端的整体评估。以下是目前业界最核心的评测指标体系：


### 检索层评估（Retrieval）


这部分主要衡量系统“找资料”的能力，即检索器是否能**准确、全面地**找到与用户问题相关的上下文信息。这个阶段，聚焦的是RAG Triad 中的**上下文相关性（Context Relevance）**


具体的评估指标有：


- **召回率（****Context** **Recall）**：衡量检索结果的“查全率”。即在所有与问题相关的文档中，系统成功检索出了多少比例。
- **精准率（****Context**** Precision）**：衡量检索结果的“查准率”。即在系统检索回来的所有文档块中，真正与问题相关的比例是多少。
- **F1分数**：**F1 Score是平衡精确率和召回率的指标**，F1分数是精确率（Precision）和召回率（Recall）的调和平均数（F1 = 2PR / (P+R)）。它同时考虑了这两者，只有当精确率和召回率都很高时，F1分数才会高。


关于召回率（Recall）和精确率（Precision）、F1 Score， 我们前面其实也讲过的。**召回率讲求的是“宁可错杀一千，不可放过一个”。精确率讲求的是：“宁缺毋滥” 。**也提到过可以使用BLEU、ROUGE、METEOR等方法来评测他们。


> **补充：F1 的命名与 F<sub>β</sub>**
>
> - **"F" 没有英文全称**：F-measure 由信息检索学者 C. J. van Rijsbergen 在 1979 年的《Information Retrieval》中推广。他先定义了 E-measure（effectiveness，有效性度量），取 F = 1 − E。字母为什么选 F 并无定论——他本人表示"只是顺手取的字母"，也有说法认为与统计学家 Ｒ. A. Fisher 有关。
> - **"1" 表示精确率与召回率等权**：F1 是通用式 F<sub>β</sub> 在 β = 1 时的特例：
>
> $$F_\beta = (1+\beta^2)\cdot\frac{P\cdot R}{\beta^2 P + R}$$
>
> β = 1 时退化为两者的调和平均数：
>
> $$F1 = \frac{2PR}{P+R}$$
>
> - **β 的调节作用**：β > 1 更看重召回率（RAG 检索场景常用，怕漏），β < 1 更看重精确率（怕噪声）。


但是，这里提到的召回率（Context Recall）、精准率（Context Precision）和之前我们提到的传统Recall、Precision还稍有不同，


- **传统的 Precision / Recall** 是**“死板”的数学统计**。它们需要人工提前标注好Ground Truth，然后机械地对比检索回来的文档 ID 对不对。
- **Context Precision / Context Recall** 是**“灵活”的语义评估**。它们利用LLM作为裁判，直接阅读文档内容，从语义层面判断检索回来的信息到底有没有用。这是RAG评测，尤其是很多评测框架中比较常用的手段——**LLM-as-a-Judge**
- **MRR（平均倒数排名）/NDCG（归一化折损累计增益）**：衡量相关文档的排名位置。如果正确答案排在越靠前的位置，得分越高。它不仅关心“有没有找到”，更关心“找得靠不靠前”。


> **补充：MRR/NDCG 低的优化手段（rerank 是核心）**
>
> **先归因**：MRR/NDCG 低 ≠ 召回率低。先看 Recall：
> - Recall 也低 → 粗排问题（embedding 差、索引差、Top-K 太小）；
> - **Recall 高但 MRR/NDCG 低 → 召回到但排名靠后 → 上 rerank**。
>
> **为什么 rerank 有效**：
>
> | 阶段 | 模型 | 精度 | 速度 |
> |:---|:---|:---|:---|
> | 粗排（向量检索） | Bi-Encoder 双塔：query 和 doc 独立编码再算相似度 | 粗 | 快（毫秒级，可预索引） |
> | 重排（rerank） | Cross-Encoder：query + doc 拼接过一次模型 | 细 | 慢（每对都要跑，不能预计算） |
>
> 粗排快但粗，rerank 慢但准——MRR/NDCG 低的本质就是粗排把相关 doc 排进 Top-20 但没进 Top-5，rerank 专治这个。
>
> **优化手段（按收益排序）**：
>
> 1. **二段式漏斗**（最常见）：粗排 Top-30~50 → rerank 精排 → 取分 ≥ 阈值的 Top-5 喂 LLM。know-engine 的实践是 `ES KNN 召回 → 0.5 粗筛 → BGE 精排 → 0.6 精筛` 三段式；
> 2. **选对 rerank 模型**：中文场景 **BGE-Reranker-v2-m3**（自部署、中英、性价比最高）；闭源选 Cohere Rerank 3 或 Jina Reranker v2；
> 3. **多路召回 + RRF 融合再 rerank**：向量召回 + BM25 召回 → RRF 融合 → rerank。注意 know-engine 踩过的坑——langchain4j `DefaultContent` 没重写 `equals/hashCode`，同 chunk 跨路被当两个 key、分数无法累加，融合前必须去重；
> 4. **调 Top-N 参数**：粗排宽（30~50）、rerank 细（5~10），输入太少召回率有天花板，太多则慢且贵；
> 5. **rerank 之外的备选**：embedding 升级（根本解但要重建索引）、query 改写（query 太短时）、HNSW `efSearch` 调大（速度换召回）、chunk 切分优化。
>
> **代价与降级**：
> - 延迟：Top-30 rerank 通常 100~500ms；
> - 成本：自部署需 GPU；API 按调用收费；
> - 降级：高负载时跳过 rerank，粗排 Top-K 直接喂 LLM，用质量换可用性。
>
> **面试答题骨架**：归因（Recall 也低 vs 只排名低）→ 上 rerank → 选型（BGE-v2-m3）→ 进阶（多路 + RRF + rerank）→ 代价（延迟 + GPU，要能降级）。


**上下文相关性（Context Relevance）****是最终的目标（即“找到的资料准不准”），而 Recall、Precision、MRR、NDCG 等，则是从不同维度去衡量这个目标达成情况的“具体尺子”**。


### 生成层评估（Generation）

这部分主要衡量系统“写答案”的能力，即大模型是否能基于检索到的信息生成高质量、无幻觉的回答。这个阶段，聚焦的是RAG Triad 中的**忠实度 / 依据性（Faithfulness / Groundedness）、****答案相关性（Answer Relevance）**


而这个阶段，指标更加聚焦，主要就是：


- **忠实度（Faithfulness）**：衡量生成的答案是否严格基于检索到的上下文，是否出现了“幻觉”（即编造了上下文中不存在或与上下文矛盾的信息）。
- **答案相关性（Answer Relevancy）**：衡量最终生成的答案是否直接、准确地回答了用户的原始问题。


除了上面这两个外，如果RAG的问题是有标准答案的，那可还可以通过以下指标评测：


- **答案正确性（Answer Correctness）**：这是衡量最终答案质量的“金标准”，它综合了“事实准确度”和“语义相似度”两个维度。它需要引入人工标注的标准答案（Ground Truth）作为参照，不仅要求生成的答案在语义表达上与标准答案相近（像不像），更要求核心事实（如数据、人名、结论）必须完全一致（对不对）。在主流的评估框架中，事实准确度的权重通常更高，以避免AI用“正确的废话”掩盖事实错误


**答案正确性（Answer Correctness）和****答案相关性（Answer Relevancy）对比：**

|  |  |  |
| --- | --- | --- |
| 维度 | **答案相关性 (Answer Relevancy)** | **答案正确性 (Answer Correctness)** |
| **核心拷问** | **“答非所问了吗？”** | **“答案说对了吗？”** |
| **评估对象** | 评估【用户问题】与【生成答案】的意图匹配度。 | 评估【生成答案】与【标准答案(Ground Truth)】的事实一致度。 |
| **是否依赖标准答案** | **不需要**标准答案。 | **必须依赖**标准答案（Ground Truth）。 |
| **计算逻辑** | 让 LLM 根据答案反向生成问题，看反推的问题和原问题语义像不像。 | 结合"语义相似度"和"事实准确度（TP/FP/FN比对）”综合打分。 |


#### GT 依赖性总览（面试高频）

> **Ground Truth（GT）**= 人工提前标注的标准答案/标准文档集。"**免 GT**" 指**不需要标准答案**，靠 LLM 当裁判直接比对 Query/Context/Response 两两关系就能算分；"**必须 GT**" 指必须先有人工标注，才能算分。

| 指标 | GT 依赖 | 说明 |
|:---|:---|:---|
| Context Precision / Recall（RAGAS 版） | **免 GT** | LLM 阅读文档内容，语义判断"有没有用" |
| Faithfulness | **免 GT** | LLM 比对 Response 与 Context，看是否幻觉 |
| Answer Relevancy | **免 GT** | LLM 由答案反推问题，与原问题算语义相似度 |
| 传统 Precision / Recall | **必须 GT** | 机械对比检索回来的文档 ID 与标注集 |
| Answer Correctness | **必须 GT** | 与标准答案做 TP/FP/FN 事实比对 |

**为什么"免 GT"重要**：

1. **成本差异**：需 GT 的指标，1000 条评测集 = 1000 条人工标注；免 GT 指标只需 query 本身，LLM 自动算分；
2. **上线速度**：免 GT 指标可拿线上真实 query 直接评；需 GT 指标必须先攒标注集才能跑；
3. **RAG Triad 的核心卖点**：TruLens 提出的 RAG Triad 三指标（Context Relevance / Faithfulness / Answer Relevance）**全部免 GT**，这是它能工业级普及的关键——也是面试常问"哪些 RAG 指标不需要标准答案"的答题骨架。


### 端到端与工程化评估

除了上述核心质量指标，工业级 RAG 系统还会关注以下维度：

- **端到端质量（End-to-end Score）**：通过 A/B 测试或构建黄金测试集，综合评估用户从提问到获取最终答案的整体体验。
- **鲁棒性与安全性**：评估系统在面对噪声文档、错误信息或恶意攻击时的稳定性，以及是否会产生有害、偏见或侵犯版权的内容。
- **性能与成本指标**：包括检索延迟（如 Embedding 生成耗时、数据库查询耗时）、QPS（每秒查询率）、GPU/CPU 利用率以及大模型 API 的调用成本等。
- **答案正确性（Answer Correctness）**：有些时候，也会把前面提到的答案正确性作为端到端评测的一个指标。
