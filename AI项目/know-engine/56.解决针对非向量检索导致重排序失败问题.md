# ✅解决针对非向量检索导致重排序失败问题

  

在 RAG 混合检索场景中，系统同时使用多种检索器：

- 向量/全文检索：Elasticsearch（返回带相似度分数的文本片段）
- 结构化查询：SQL 检索器、Cypher 检索器（返回精确的表格数据）

当这些结果进入 KnowEngineReRankingContentAggregator 进行 RRF 融合和 BGE 重排序时，结构化查询结果会导致重排序失败或效果异常，原因包括：

- 评分模型不适用：BGE scoring model 是为文本相似度设计的，对 SQL/Cypher 返回的结构化表格数据评分不准确
- RRF 融合不合理：结构化查询结果是精确匹配，不需要与向量检索结果做倒数秩融合
- 代码中基于chunk的embedding\_id做去重，而结构化查询没这个字段，会报错。

  

于是，我们设计了一个基于元数据标记的混合聚合架构，让结构化结果跳过重排序/融合，直接透传给 LLM。

  

### 定义 skipRerank 标记常量

MetadataKeyConstant.java

  

```
public static final String SKIP_RERANK = "skipRerank";
```

  

### SQL/Cypher 检索器为成功结果打标记

  

KnowEngineNeo4jContentRetriever.java、KnowEngineSqlDatabaseContentRetriever.java

  

```
@Overridepublic List<Content> retrieve(Query query) {    List<Content> results;    try {        // 执行 Cypher 查询        results = neo4jText2CypherRetriever.retrieve(query);    } catch (Exception e) {        // 异常时降级到向量检索（不带标记）        return fallbackRetriever.retrieve(query);    }﻿    // 空结果也降级    if (results == null || results.isEmpty() || isCypherResultEmpty(results)) {        return fallbackRetriever.retrieve(query);    }﻿    // ✅ 成功结果标记 skipRerank，直接透传    return results.stream()            .map(ContentUtil::markAsSkipRerank)            .collect(Collectors.toList());}
```

  

### 创建混合聚合器，分离两类结果

  

KnowEngineHybridContentAggregator.java

  

```
public class KnowEngineHybridContentAggregator implements ContentAggregator {﻿    private final ContentAggregator unstructuredAggregator;  // 原有的重排序聚合器﻿    @Override    public List<Content> aggregate(Map<Query, Collection<List<Content>>> queryToContents) {        List<Content> structuredContents = new ArrayList<>();        Map<Query, Collection<List<Content>>> unstructuredQueryToContents = new LinkedHashMap<>();﻿        // 🔍 第一步：遍历所有结果，按 skipRerank 标记分离        for (Map.Entry<Query, Collection<List<Content>>> entry : queryToContents.entrySet()) {            Query query = entry.getKey();            Collection<List<Content>> contentLists = entry.getValue();﻿            List<List<Content>> unstructuredLists = new ArrayList<>();            for (List<Content> contents : contentLists) {                List<Content> unstructured = new ArrayList<>();                for (Content content : contents) {                    if (ContentUtil.isSkipRerank(content)) {                        structuredContents.add(content);  // ✅ 结构化结果：直接收集                    } else {                        unstructured.add(content);        // 📝 非结构化结果：待重排序                    }                }                if (!unstructured.isEmpty()) {                    unstructuredLists.add(unstructured);                }            }﻿            if (!unstructuredLists.isEmpty()) {                unstructuredQueryToContents.put(query, unstructuredLists);            }        }﻿        // 🔄 第二步：只对非结构化结果调用 RRF + BGE 重排序        List<Content> unstructuredResults =             unstructuredAggregator.aggregate(unstructuredQueryToContents);﻿        // 📦 第三步：合并结果（结构化在前，非结构化在后）        List<Content> combined = new ArrayList<>(            structuredContents.size() + unstructuredResults.size());        combined.addAll(structuredContents);        combined.addAll(unstructuredResults);        return combined;    }}﻿
```

  

### 在 ChatApplicationService 中组装聚合器链

  

```
// 使用带进度通知的聚合器包装原始聚合器// 混合聚合器：SQL/Cypher 结构化结果直接透传，仅对向量/全文检索结果做 RRF 融合和重排序ContentAggregator contentAggregator = new ProgressAwareContentAggregator(    new KnowEngineHybridContentAggregator(          // ← 新增：混合聚合器        KnowEngineReRankingContentAggregator.builder()            .scoringModel(scoringModel)             // BGE 重排序模型            .minScore(0.6)            .maxResults(5)            .querySelector(queryToContents -> queryToContents.keySet().iterator().next())            .build()    ),    processCallback, chatParam.assistantMessageId(), chatMessageService);﻿
```

  

  

[✅解决针对非向量检索导致重排序失败问题](https://thoughts.aliyun.com/workspaces/6963289eb0fc2e001bb052eb/docs/6a3fa4aa51b1440001d3bce0#slate-title)

[定义 skipRerank 标记常量](https://thoughts.aliyun.com/workspaces/6963289eb0fc2e001bb052eb/docs/6a3fa4aa51b1440001d3bce0#6a3fabd66a229123c59ea5e4)

[SQL/Cypher 检索器为成功结果打标记](https://thoughts.aliyun.com/workspaces/6963289eb0fc2e001bb052eb/docs/6a3fa4aa51b1440001d3bce0#6a3fabf96a229156fc9ea5ea)

[创建混合聚合器，分离两类结果](https://thoughts.aliyun.com/workspaces/6963289eb0fc2e001bb052eb/docs/6a3fa4aa51b1440001d3bce0#6a3fac1d6a229187b49ea5f1)

[在 ChatApplicationService 中组装聚合器链](https://thoughts.aliyun.com/workspaces/6963289eb0fc2e001bb052eb/docs/6a3fa4aa51b1440001d3bce0#6a3fac5e6a22910a399ea5fb)

![](./assets/no-search-result.png)

你可以将零散的思绪与文字先整理为草稿
