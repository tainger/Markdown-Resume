# 160. 相交链表

> **难度**：🟢 简单 | **标签**：哈希表、链表、双指针 | **企业**：—

## 题目描述

给你两个单链表的头节点 `headA` 和 `headB` ，请你找出并返回两个单链表相交的起始节点。如果两个链表不存在相交节点，返回 `null` 。

图示两个链表在节点 `c1` 开始相交：

```
a1 → a2
        ↘
          c1 → c2 → c3
        ↗
b1 → b2 → b3
```

题目数据**保证**整个链式结构中不存在环。函数返回结果后，链表必须保持其原始结构。

## 示例

**示例 1**

```
输入：intersectVal = 8, listA = [4,1,8,4,5], listB = [5,6,1,8,4,5], skipA = 2, skipB = 3
输出：Intersected at '8'

listA:  4 → 1
              ↘
                8 → 4 → 5
              ↗
listB:  5 → 6 → 1
```

**示例 2**

```
输入：intersectVal = 2, listA = [1,9,1,2,4], listB = [3,2,4], skipA = 3, skipB = 1
输出：Intersected at '2'

listA:  1 → 9 → 1
                  ↘
                    2 → 4
                  ↗
listB:        3
```

**示例 3**

```
输入：intersectVal = 0, listA = [2,6,4], listB = [1,5], skipA = 3, skipB = 2
输出：null

listA:  2 → 6 → 4
listB:  1 → 5
两链表不相交
```

---

## 提示

- `listA` 中节点数目为 `m`
- `listB` 中节点数目为 `n`
- \\(1 \\le m, n \\le 3 \\times 10^4\\)
- \\(1 \\le \\text{Node.val} \\le 10^5\\)
- `0 <= skipA <= m`
- `0 <= skipB <= n`
- 如果 `listA` 和 `listB` 没有交点，`intersectVal` 为 `0`
- 如果 `listA` 和 `listB` 有交点，`intersectVal == listA[skipA] == listB[skipB]`

---

## 进阶

你能否设计一个时间复杂度 \\(O(m + n)\\)、仅用 \\(O(1)\\) 内存的解决方案？

---

## 思路

### 核心思想

两个指针分别从 A 和 B 出发，走到尽头后**切换到对方链表的头部**继续走。如果相交，两个指针走过的总路程相等，会在交点相遇；如果不相交，都会走到 null。

```
关键洞察：消除长度差

listA:  a1 → a2 → c1 → c2 → c3    长度 = 5
listB:  b1 → b2 → b3 → c1 → c2 → c3  长度 = 6

指针 pA 路径：a1→a2→c1→c2→c3→b1→b2→b3→c1   ← 在 c1 相遇
指针 pB 路径：b1→b2→b3→c1→c2→c3→a1→a2→c1   ← 在 c1 相遇

pA 走: A独有(2) + 共有(3) + B独有(3) = 8
pB 走: B独有(3) + 共有(3) + A独有(2) = 8
                     ↑
              两者路程相同，必然同时到达相交点
```

### 解法一：哈希表（空间 O(n)）

1. 遍历链表 A，所有节点存入 HashSet
2. 遍历链表 B，第一个在 Set 中出现的节点即为交点

- 时间复杂度：\\(O(m+n)\\)，空间复杂度：\\(O(m)\\)

### 解法二：双指针（空间 O(1)）

1. `pA = headA`, `pB = headB`
2. 每步 `pA` 和 `pB` 同时前进一位
3. `pA` 走到 null → 重定向到 `headB`；`pB` 走到 null → 重定向到 `headA`
4. 若 `pA == pB`，该节点即为交点；若都为 null，说明不相交

- 时间复杂度：\\(O(m+n)\\)，空间复杂度：\\(O(1)\\)

> ⚠️ 注意是 `pA == pB`（引用相等），不是 `pA.val == pB.val`。

---

## 代码

### 我的伪代码

```java
public class Solution {
    public ListNode getIntersectionNode(ListNode headA, ListNode headB) {
        // 请在此写下你自己的思路 / 伪代码
        // 后续我们会一起分析问题并标注
    }
}
```

### 解法一：哈希表

```java
public ListNode getIntersectionNode(ListNode headA, ListNode headB) {
    Set<ListNode> visited = new HashSet<>();
    ListNode p = headA;
    while (p != null) {
        visited.add(p);
        p = p.next;
    }
    p = headB;
    while (p != null) {
        if (visited.contains(p)) return p;
        p = p.next;
    }
    return null;
}
```

### 解法二：双指针（推荐，O(1) 空间）

```java
public ListNode getIntersectionNode(ListNode headA, ListNode headB) {
    if (headA == null || headB == null) return null;

    ListNode pA = headA, pB = headB;
    while (pA != pB) {
        // 走到尽头就切到对方链表头部
        pA = (pA == null) ? headB : pA.next;
        pB = (pB == null) ? headA : pB.next;
    }
    // pA == pB 时：要么是交点，要么都是 null（不相交）
    return pA;
}
```

---

## 个人总结

<!-- 待填写：写下你写伪代码时踩的坑、卡住的点，我们会一起分析并总结 -->
