# 5. 最长回文子串

> **难度**：🟡 中等 | **标签**：字符串、动态规划 | **企业**：—（Hot100 第 5 题）

## 刷题记录

| # | 日期 | 解法 | 复杂度 | 状态 | 备注 |
|:---:|:---|:---|:---|:---|:---|
| 1 | 2026-08-29 | 解法一：暴力递归 \(O(n^3)\)（TLE）；解法二：记忆化搜索 \(O(n^2)\)；解法三：区间 DP \(O(n^2)\)；解法四：中心扩展 \(O(n^2)\) 空间 \(O(1)\)（推荐） | \(O(n^2)\) | ⬜ 未完成 | 待刷，回文子串模板题：递归→记忆化→DP 一条链练透，中心扩展是面试标准答案，Manacher \(O(n)\) 是加分项 |
| 2 | 2026-08-31 | 解法五：for 循环枚举长度 + 滑动窗口（长度从大到小枚举，发现回文直接 return） | 最坏 \(O(n^3)\) / 平均快于暴力 | ⬜ 未完成 | 练习用：直觉好写，但判回文仍是 \(O(n)\)，面试不是最优解，需知道它的局限性 |

> 状态约定：⬜ 未刷 · 🟡 已刷待复习 · ✅ 二刷通过

## 题目描述

给你一个字符串 `s`，找到 `s` 中最长的**回文子串**。

**子串**是字符串中**连续**的字符序列（区别于可不连续的「子序列」）。

## 示例

**示例 1**

```
输入：s = "babad"
输出："bab"
解释："aba" 同样是符合题意的答案。
```

**示例 2**

```
输入：s = "cbbd"
输出："bb"
```

---

## 提示

- `1 <= s.length <= 1000`
- `s` 仅由数字和英文字母组成

---

## 进阶

你能给出 \(O(n)\) 复杂度的解法吗？（Manacher 算法，见 [个人总结](#个人总结) 第 7 点）

---

## 思路

### 核心：枚举「回文的形状」，而不是枚举所有子串

暴力枚举所有子串再逐个判回文是 \(O(n^3)\)。优化思路是利用回文的**递推结构**：

> 一个串是回文 ⇔ **首尾字符相等**，且**去掉首尾后的内部子串也是回文**。

「大回文由小回文推出」——这就是区间 DP；沿另一条路走：从每个可能的**回文中心**向两边扩展——这就是中心扩展。

```
b a b a d
    ↑ 中心 = 'b'，向两边扩：a==a ✓ → 继续扩越界停止 → "aba"（奇长度）
      ↑↑ 中心 = "bb" 间隙（cbbd 中）→ 向两边扩 → "bb"（偶长度）
```

### 解法链总览：暴力递归 → 记忆化 → 递推 DP（先笨后巧）

三版解法的**递归定义完全一致**：`isPal(i, j) = s[i]==s[j] && (j-i<3 || isPal(i+1, j-1))`，区别只在「算过的结果存不存、怎么取」：

| 版本 | 结果存哪 | 取结果方式 | 时间 | 备注 |
|:---|:---|:---|:---|:---|
| 解法一：暴力递归 | 不存 | 每次从头重算 | \(O(n^3)\) | 会 TLE，但定义清晰，是后续优化的地基 |
| 解法二：记忆化搜索 | `memo[i][j]` | 自顶向下，递归前查缓存 | \(O(n^2)\) | 「自顶向下的 DP」，无遍历顺序烦恼 |
| 解法三：递推 DP | `dp[i][j]` | 自底向上，循环顺序保证依赖就绪 | \(O(n^2)\) | 去掉递归调用栈 |
| 解法四：中心扩展 | 不存 | 换枚举维度：按中心向外扩 | \(O(n^2)\) / \(O(1)\) 空间 | 面试标准答案 |
| 解法五：for 枚举长度 + 滑动窗口 | 不存 | 枚举长度从大到小，窗口滑动判回文 | 最坏 \(O(n^3)\) | 直觉好写，练手感用；面试不够优 |

> 「递归 → 记忆化 → DP」的通用套路链见 [算法思想/0.递归](../算法思想/0.递归.md) 与 [1.动态规划](../算法思想/1.动态规划.md)。

### 解法三：动态规划（自底向上递推，好想）

#### 推导链：回文定义 → 翻译成 DP 记号 → 补边界 → 合并方程

**第一步：从回文的原始定义出发**

回文：顺着读和倒着读一模一样。对子串 `s[i..j]`（闭区间）来说，其成立条件可形式化为：

$$
\text{isPal}(i, j) \iff (s[i] = s[j]) \,\wedge\, \text{isPal}(i+1, j-1)
$$

这是递推关系的雏形——「大区间」的答案由「小区间」的答案推出。

**第二步：把伪代码翻译成 DP 记号**

定义 $dp[i][j] = \text{true}$ 表示 $s[i..j]$ 是回文。逐词替换：

| 伪代码 | 翻译 |
|:---|:---|
| $s[i] = s[j]$ | `s.charAt(i) == s.charAt(j)` |
| $s[i+1..j-1]$ 也是回文 | $dp[i+1][j-1]$ |

得到第一版转移：

$$
dp[i][j] = (s[i] = s[j]) \,\wedge\, dp[i+1][j-1]
$$

但有漏洞：区间很短时，$s[i+1..j-1]$ 根本不存在（或只剩单字符，天然是回文），不需要再查 $dp$。

**第三步：补边界——什么时候「不用查内部」？**

按区间长度 `len = j - i + 1` 枚举：

| len | j-i | 区间 | 是否天然回文？ | 需查 `dp[i+1][j-1]`？ |
|:---:|:---:|:---|:---:|:---:|
| 1 | 0 | `"a"` 单字符 | ✅ 是 | ❌ 不用查 |
| 2 | 1 | `"aa"` 双字符 | 两端相等即天然成立（无内部） | ❌ 不用查 |
| 3 | 2 | `"aba"` 三字符 | 两端相等即成立（中间单字符天然回文） | ❌ 不用查 |
| ≥4 | ≥3 | `"abba"`/`"abcba"` | 要看内部 | ✅ 必须查 |

临界点：当 $j - i < 3$ 时（$\text{len} \le 3$），只要两端字符相等，就一定是回文——内部无需再判。形式化：

$$
\text{len} \le 3 \;\Longrightarrow\; (s[i] = s[j]) \Rightarrow s[i..j] \text{ 是回文}
$$

> 为什么是 $< 3$ 不是 $< 2$？因为 $\text{len}=3$（$j-i=2$）时中间只剩一个字符，单字符天然是回文——这就是边界取 3 的由来。

**第四步：合并边界条件 → 最终方程**

把「天然成立的短区间」作为析取（OR）的左项，得到最终状态转移方程：

$$
\boxed{\, dp[i][j] = (s[i] = s[j]) \,\wedge\, \big(\, (j - i < 3) \;\vee\; dp[i+1][j-1] \,\big) \,}
$$

对应代码那行：

```java
if (j - i < 3 || dp[i + 1][j - 1]) {    // 短区间 或 内部回文
    dp[i][j] = true;
}
```

注意优先级：$(j - i < 3)$ 写在 $\vee$ 左项，命中即短路，跳过 $dp[i+1][j-1]$——此时若 $i+1 > j-1$（$\text{len}=2$ 时 $i+1=j$、$j-1=i$），左下标会「反」，但短路求值保证不读那个不存在的状态，避免数组越界。

#### 等价表达：分段式方程（base case 显式拆分）

同一个方程还可以把边界**显式拆成 base case**、转移只管 $\text{len} \ge 3$——这是「递归终止条件 → base case、递归体 → 转移」的机械翻译：

$$
dp[i][j] = \begin{cases} \text{true} & i = j \;\,(\text{len}=1,\ \text{单字符天然回文}) \\ s[i] = s[j] & j = i+1 \;\,(\text{len}=2,\ \text{没有内部}) \\ s[i] = s[j] \,\wedge\, dp[i+1][j-1] & j - i \ge 2 \;\,(\text{len} \ge 3) \end{cases}
$$

两个必须点破的细节：

1. **len=3 走第三段，靠 base case 兜底**：len=3 时 $dp[i+1][j-1] = dp[i+1][i+1]$ 是单字符格，只有「len=1 全表预填 true」先执行，转移才成立——合并式的 $j-i<3$ 短路与分段式的 base case 预填，是**同一件事的两种落位**，两者完全等价；
2. **边界范围别说错**：$j-i<3$ 覆盖 $j-i \in \{0,1,2\}$，对应 **len = 1、2、3 三种**（$\text{len} = j-i+1$），不是两种。

> 通用套路：**暴力定义 → 翻译成 DP 记号 → 枚举短区间补边界 → 合并成 OR 或拆成 base case 分段式（两种等价落位）**。这与 [53. 最大子数组和](53.%20最大子数组和.md) 第 2 点「暴力分组 → 暴露重复子结构 → 转移方程自己浮出来」是同一套推导纪律——先写出最直白的依赖，再补边界与短路保护。

#### 四要点

1. 定义 `dp[i][j]`：子串 `s[i..j]` 是否为回文
2. 转移：`dp[i][j] = (s[i] == s[j]) && (j - i < 3 || dp[i+1][j-1])`
   - `j - i < 3`（长度 1/2/3）时内部无子串或单字符，首尾相等即回文
3. 遍历顺序：`dp[i][j]` 依赖**左下角** `dp[i+1][j-1]` → **i 从大到小、j 从小到大**（或按子串长度从小到大枚举）
4. 过程中用 `s[i..j]` 是回文时更新最长起点和长度

- 时间复杂度：\(O(n^2)\)，空间复杂度：\(O(n^2)\)（DP 表）

### 解法四：中心扩展（推荐）

1. 回文中心共 **2n−1 个**：n 个字符（奇长度回文）+ n−1 个字符间隙（偶长度回文）
2. 对每个中心，双指针 `l/r` 向两边扩：`s[l] == s[r]` 就继续，否则停
3. 记录扩展出的最长回文的**起点和长度**，最后 `substring` 截取

- 时间复杂度：\(O(n^2)\)（n 个中心 × 每次最多扩 n/2），空间复杂度：\(O(1)\)

### 解法五：for 循环枚举长度 + 滑动窗口（练手用）

思路很直接：**从最长的可能长度开始，固定窗口长度扫一遍，找到第一个回文就直接返回**。

1. 外层 `for`：长度 `len` 从 `n` 倒序到 `1`（最长优先，找到就 return）；
2. 内层窗口：左端点 `i` 从 `0` 到 `n - len`，每个长度为 `len` 的窗口对应子串 `s[i..i+len-1]`；
3. 窗口内判回文：双指针两端对撞（\(O(len)\)）；
4. 找到第一个回文窗口 → 直接返回（因为我们从最长的开始枚举，第一个命中的就是全局最长）。

```
s = "babad", n = 5

len=5：窗口 s[0..4] = "babad" → 判回文 ✗
len=4：窗口 s[0..3]="baba" ✗、s[1..4]="abad" ✗
len=3：窗口 s[0..2]="bab" ✓ → 直接返回 "bab"（后面的 len 不用再看）
```

- **最优情况**：整串就是回文（`len=n` 直接命中），时间 \(O(n)\)；
- **平均情况**：比暴力枚举所有子串再判回文**快一截**——因为一找到就收手，不需要把所有 \(O(n^2)\) 个子串全扫完；
- **最坏情况**：长 1 的单字符才是答案（如 `"abcde"`），要扫完所有长度，每个窗口判回文 \(O(n)\)，时间退化为 **\(O(n^3)\)**，`n=1000` 勉强擦边但仍可能 TLE；
- 空间复杂度：\(O(1)\)。

> 关键巧思：**长度从大到小枚举**，找到直接 return。如果从小到大枚举再比较长度，就彻底退化成暴力 \(O(n^3)\) 且没有任何提前收手的可能。

---

## 代码

### 解法一：暴力递归（TLE，练手 + 验证正确性用）

```java
class Solution {
    int start = 0, maxLen = 1;

    public String longestPalindrome(String s) {
        for (int i = 0; i < s.length(); i++)
            for (int j = i; j < s.length(); j++)
                if (isPal(s, i, j) && j - i + 1 > maxLen) {   // 每个区间都问一遍：是回文吗
                    start = i;
                    maxLen = j - i + 1;
                }
        return s.substring(start, start + maxLen);
    }

    // 递归判 s[i..j]：首尾相等 && 内部也是回文（递归出口：长度 ≤ 3）
    private boolean isPal(String s, int i, int j) {
        if (j - i < 3) return s.charAt(i) == s.charAt(j);
        return s.charAt(i) == s.charAt(j) && isPal(s, i + 1, j - 1);
    }
}
```

- \(O(n^2)\) 个区间 × 每次递归判定最多 \(O(n)\) 层 = \(O(n^3)\)，`n=1000` 时约 \(10^9\) 级操作，**LeetCode 会超时**。
- 价值在于：`isPal` 这行递归定义就是后续记忆化 / DP 的**唯一核心逻辑**，先把最笨的写对，优化只动「缓存」不动「逻辑」。

### 解法二：记忆化搜索（自顶向下的 DP）

```java
class Solution {
    int start = 0, maxLen = 1;
    Boolean[][] memo;                            // Boolean 三态：null = 还没算过

    public String longestPalindrome(String s) {
        int n = s.length();
        memo = new Boolean[n][n];
        for (int i = 0; i < n; i++)
            for (int j = i; j < n; j++)
                if (isPal(s, i, j) && j - i + 1 > maxLen) {
                    start = i;
                    maxLen = j - i + 1;
                }
        return s.substring(start, start + maxLen);
    }

    private boolean isPal(String s, int i, int j) {
        if (j - i < 3) return s.charAt(i) == s.charAt(j);
        if (memo[i][j] != null) return memo[i][j];            // ★ 查缓存，命中直接返回
        boolean res = s.charAt(i) == s.charAt(j) && isPal(s, i + 1, j - 1);
        return memo[i][j] = res;                              // ★ 存缓存（赋值表达式返回值即 res）
    }
}
```

- 与暴力递归**只差两行**（查缓存 + 存缓存），重复子问题从指数级降到 \(O(n^2)\) 个状态，时间 \(O(n^2)\)、空间 \(O(n^2)\) + 递归栈 \(O(n)\)。
- **不用纠结遍历顺序**：递归天然「先算内部、再算外层」，这正是它比递推 DP 好写的地方。

### 解法三：动态规划（自底向上递推）

```java
class Solution {
    public String longestPalindrome(String s) {
        int n = s.length();
        boolean[][] dp = new boolean[n][n];      // dp[i][j]: s[i..j] 是否回文
        int start = 0, maxLen = 1;               // 至少整串有单字符回文

        for (int i = n - 1; i >= 0; i--) {       // i 从大到小：先算出左下角 dp[i+1][*]
            for (int j = i; j < n; j++) {
                if (s.charAt(i) != s.charAt(j)) continue;     // 首尾不等，必非回文
                if (j - i < 3 || dp[i + 1][j - 1]) {          // 长度≤3 或 内部是回文
                    dp[i][j] = true;
                    if (j - i + 1 > maxLen) {                 // 更新最长回文
                        start = i;
                        maxLen = j - i + 1;
                    }
                }
            }
        }
        return s.substring(start, start + maxLen);
    }
}
```

**变体写法：按长度枚举填表（个人总结第 3 点方案二的落地版）**

与推导链的分段式方程**三段严格镜像**：base case 两个循环预填，转移只管 len ≥ 3：

```java
class Solution {
    public String longestPalindrome(String s) {
        int n = s.length();
        boolean[][] dp = new boolean[n][n];
        int start = 0, maxLen = 1;

        // base case ①：len=1 全表预填（单字符天然回文）——len=3 的转移靠它兜底
        for (int i = 0; i < n; i++) dp[i][i] = true;

        // base case ②：len=2，两端相等即回文
        for (int i = 0; i + 1 < n; i++) {
            if (s.charAt(i) == s.charAt(i + 1)) {
                dp[i][i + 1] = true;
                if (maxLen < 2) { start = i; maxLen = 2; }
            }
        }

        // 转移：len 从 3 到 n，依赖格 dp[i+1][j-1] 长度为 len-2，必已算好（拓扑序显式）
        for (int len = 3; len <= n; len++) {
            for (int i = 0; i + len - 1 < n; i++) {
                int j = i + len - 1;
                if (s.charAt(i) == s.charAt(j) && dp[i + 1][j - 1]) {
                    dp[i][j] = true;
                    if (len > maxLen) {
                        start = i;
                        maxLen = len;
                    }
                }
            }
        }
        return s.substring(start, start + maxLen);
    }
}
```

- 与 i 倒序版**转移逻辑一字不差**，区别只在枚举维度：按长度显式满足拓扑序，换任何区间 DP（516 回文子序列 / 32 最长有效括号 / 石子合并）都是这个骨架，零出错率；
- 代价：多两个 base case 循环 + 每层手算 `j = i + len - 1`——用代码量换「不用想循环方向」。

### 解法四：中心扩展（推荐）

```java
class Solution {
    public String longestPalindrome(String s) {
        int n = s.length(), start = 0, maxLen = 1;

        for (int k = 0; k < n; k++) {
            // 奇长度：中心是字符 s[k] 本身
            int[] r1 = expand(s, k, k);
            // 偶长度：中心是 s[k] 与 s[k+1] 的间隙
            int[] r2 = expand(s, k, k + 1);
            for (int[] r : new int[][]{r1, r2}) {
                if (r[1] > maxLen) {
                    maxLen = r[1];
                    start = r[0];
                }
            }
        }
        return s.substring(start, start + maxLen);
    }

    // 从中心 l/r 向两边扩展，返回 {最长回文起点, 长度}
    private int[] expand(String s, int l, int r) {
        while (l >= 0 && r < s.length() && s.charAt(l) == s.charAt(r)) {
            l--; r++;                        // 匹配成功，继续向外扩
        }
        // 退出时 s[l] != s[r]（或越界），回文实际是 (l, r) 开区间内
        int len = r - l - 1;
        return new int[]{l + 1, len};
    }
}
```

### 解法五：for 循环枚举长度 + 滑动窗口

```java
class Solution {
    public String longestPalindrome(String s) {
        int n = s.length();

        // 外层：长度从大到小枚举，第一个命中即全局最长，直接 return
        for (int len = n; len >= 1; len--) {
            // 内层：窗口左端点 i，窗口范围 [i, i+len)
            for (int i = 0; i + len <= n; i++) {
                // 判断 s[i..i+len-1] 是否为回文
                if (isPal(s, i, i + len - 1)) {
                    return s.substring(i, i + len);   // ★ 找到即全局最长，提前终止
                }
            }
        }
        return "";   // 不会执行到（len=1 时每个字符本身就是回文）
    }

    // 双指针两端对撞，判 s[l..r] 是否回文 O(r-l+1)
    private boolean isPal(String s, int l, int r) {
        while (l < r) {
            if (s.charAt(l) != s.charAt(r)) {
                return false;
            }
            l++; r--;
        }
        return true;
    }
}
```

- **巧思就在 `return s.substring(...)` 这行**：枚举长度从 n→1，只要当前长度能找到一个回文窗口，它就必然是全局最长，直接返回不看后面——这也是为什么它平均表现比暴力快得多。
- 但**最坏仍是 \(O(n^3)\)**：比如 `"abcde"` 这种每个窗口都得判完才返回 false，对 `len=n` 有 1 个窗口 → `len=n-1` 有 2 个 → … → `len=1` 有 n 个，每个窗口判回文 \(O(n)\)，总和 \(\sum_{k=1}^{n} k \cdot O(n) = O(n^3)\)。

---

## 个人总结

### 1. 题眼：连续子串 + 回文结构 → DP 或中心扩展，\(O(n^2)\) 是面试预期

`n ≤ 1000` 说明 \(O(n^2)\) 可过（约 \(10^6\) 次操作）。回文有两层结构可利用：**区间可递推**（DP）与**中心对称**（中心扩展）。中心扩展不占额外空间，是面试的标准答案；DP 的价值在于它的转移方程是很多回文题（647、516、132）的公共底座。

### 2. 一条链练透：暴力递归 → 记忆化 → 递推 DP

三版 `isPal(i, j)` 定义**一字不改**，优化的只是「重复计算」：
- **暴力递归**：每次重算 → \(O(n^3)\) TLE，但先用它把「正确」做实；
- **记忆化搜索**：给递归加 `memo`（查缓存 + 存缓存两行）→ \(O(n^2)\)，**自顶向下**，循环顺序免想；
- **递推 DP**：把 `memo` 换成 `dp`、递归换成倒序循环 → \(O(n^2)\)，**自底向上**，省递归栈，但必须手动保证「依赖先算好」（见下一点）。

> 选型经验：依赖方向明显（如本题左下角）直接写递推 DP；依赖复杂、循环序不好定时，先写记忆化搜索，必要时再手动转 DP。通用方法论见 [算法思想/0.递归](../算法思想/0.递归.md)、[1.动态规划](../算法思想/1.动态规划.md)。

### 3. DP 的遍历顺序：由转移方程唯一决定，不是玄学

状态转移方程是：

$$
dp[i][j] = (s[i] = s[j]) \,\wedge\, \big(\, (j - i < 3) \;\vee\; \underbrace{dp[i+1][j-1]}_{\text{依赖格：左下斜对角}}\,\big)
$$

每个要算的 `dp[i][j]`，答案直接取决于它的 ↙ 左下斜对角那格。把这画成 dp 表（i 行、j 列，三角区 i≤j）：

```
          j=0  j=1  j=2  j=3  j=4
         ┌────┬────┬────┬────┬────┐
  i=0    │0,0 │0,1 │0,2 │0,3 │0,4 │  ← 算 0,4，需要 ↙  1,3
         ├────┼────┼────┼────┼────┤
  i=1    │    │1,1 │1,2 │1,3 │1,4 │  ← 算 1,3，需要 ↙  2,2
         ├────┼────┼────┼────┼────┤
  i=2    │    │    │2,2 │2,3 │2,4 │  ← len≤3 命中短区间，不读依赖格
         ├────┼────┼────┼────┼────┤
  i=3    │    │    │    │3,3 │3,4 │
         ├────┼────┼────┼────┼────┤
  i=4    │    │    │    │    │4,4 │
         └────┴────┴────┴────┴────┘
```

**设计原则（充要条件）**：**算格子 X 时，X 的 ↙ 那格已经算过**。满足的顺序有两种，都合法：

#### 方案一：i 从大到小、j 从小到大（本笔记写法）

落实到 dp 表的填入顺序：

```
第 1 轮 i=4：填 4,4                              （底行，短区间，无依赖）
第 2 轮 i=3：填 3,3 → 3,4                        （3,4 依赖 ↙ 4,3 越界，命中短区间 ✓）
第 3 轮 i=2：填 2,2 → 2,3 → 2,4                  （2,4 依赖 ↙ 3,3，i=3 轮已算好 ✓）
第 4 轮 i=1：填 1,1 → 1,2 → 1,3 → 1,4            （1,4 依赖 ↙ 2,3，i=2 轮已算好 ✓）
第 5 轮 i=0：填 0,0 → 0,1 → 0,2 → 0,3 → 0,4      （0,4 依赖 ↙ 1,3，i=1 轮已算好 ✓）
```

为什么 i 必须倒序：i 代表区间起点，依赖的是起点更靠右（i+1）的区间——**要让右边先算好，i 就得从右往左填**。i 正序的话，算 `dp[0][4]` 时 `dp[1][3]` 还没算，读的全是默认 false → 结果全错。j 从小到大是因为同一行内，下一行整个都算好了，j 序天然满足。

#### 方案二：按子串长度枚举（更直观，推荐）

按区间长度 len = 1 → 2 → 3 → … 从小到大填，同一 len 下枚举左端点 i，j = i + len - 1：

```
len=1：(0,0) (1,1) (2,2) (3,3) (4,4)    （单字符，无依赖）
len=2：(0,1) (1,2) (2,3) (3,4)          （双字符，j-i=1 < 3，命中短区间）
len=3：(0,2) (1,3) (2,4)                 （三字符，j-i=2 < 3，命中短区间）
len=4：(0,3) (1,4)                       （0,3 依赖 ↙ 1,2，len=2 已算好 ✓）
len=5：(0,4)                             （0,4 依赖 ↙ 1,3，len=3 已算好 ✓）
```

为什么合法：**`dp[i][j]` 依赖的 `dp[i+1][j-1]` 的长度比自己少 2**（去掉首尾两个字符）。按长度从小到大填——**依赖格的长度一定更小，必更早填完**。这是显式满足拓扑序的顺序，零出错率（完整代码见「代码 → 解法三 → 变体写法」，与推导链分段式方程逐段对应）。

#### 两种顺序对比 + 证伪表

| 顺序 | 优点 | 缺点 | 适用场景 |
|:---|:---|:---|:---|
| i 倒序 + j 正序 | 两层 for 紧凑，代码少 | 需要记住「左下角依赖」，换题转移方向变了容易写错 | 本题这种依赖方向简单、写熟了的场景 |
| 按长度枚举 | **拓扑序显式**，所有区间 DP 通用，零出错率 | 多一层「j = i+len-1」的心智负担 | **通用模板**：回文、516 子序列、石子合并、32 最长括号… |

**常见错误顺序证伪**：

| 假想顺序 | 错在哪里 |
|:---|:---|
| i 正序 + j 任意 | i=0 行填的时候，i+1 行还没算 → 全错 |
| 按长度倒序（5→4→3→…→1） | len=5 依赖 len=3，还没算 → 错 |
| i 倒序 + j 倒序 | 其实也合法（下一行整个都算好），但不符合「j 从 i 开始往尾走」的直觉，没人这么写 |

> 一句话：**遍历顺序的设计原则 = 把每个格子的依赖箭头全部指向已经算过的区域**。和拓扑排序同一个要求——「先算依赖、后算本体」。

### 4. 中心扩展的关键细节：中心有 2n−1 个，不是 n 个

奇长度回文（"aba"）中心是**字符**，偶长度（"bb"）中心是**两个字符之间的间隙**。漏掉偶长度中心，`"cbbd"` 会返回单字符而非 `"bb"`——这是本题第一大坑。写法上用 `expand(s, k, k)` 和 `expand(s, k, k+1)` 两次调用统一处理，避免复制两份扩展逻辑。

### 5. 记「起点 + 长度」，别只记长度

最后要返回**子串**，必须同时记录 `start` 和 `maxLen`。扩展函数退出时 `l`、`r` 都已多走一步（`s[l] != s[r]` 或越界），所以回文是**开区间** `(l, r)`：长度 = `r - l - 1`，起点 = `l + 1`。这两个「减一/加一」最容易写错，手推一遍 `"babad"`（中心 k=1 时 l 从 1 扩到 −1、r 从 1 扩到 3，len = 3−(−1)−1 = 3 → "bab"）即可验证。

### 6. 子串 vs 子序列：一字之差两道题

| 对比 | 5. 最长回文**子串** | 516. 最长回文**子序列** |
|:---|:---|:---|
| 连续性 | 必须连续 | 可不连续 |
| 解法 | 中心扩展 / 区间 DP | 区间 DP（`dp[i][j]` = 区间内最长回文子序列长度） |
| 空间 | 中心扩展可 \(O(1)\) | 必须 \(O(n^2)\) |

### 7. for 循环 + 滑动窗口的优劣：好写但不够优，面试要说明取舍

**为什么它好写**：完全符合直觉——「要最长的，就从最长的长度开始试，试到对就交卷」。两层 for + 一个判回文函数，三分钟就能写完。

**为什么它不够优**：**瓶颈在「判回文仍要 O(n)」**——窗口从一端滑到另一端，相邻两个窗口只差一个字符，但判回文不能复用之前的结论，每个窗口都要双指针对撞扫一遍。这是它和真正的「滑动窗口（双端单调，增量维护窗口信息）」的本质区别：

| 真正的滑动窗口（如 [3. 无重复最长子串](3.%20无重复字符的最长子串.md)） | 本题「固定长度 + 判回文」 |
|:---|:---|
| 窗口滑动时 **O(1) 增量维护**（进一个字符、出一个字符、更新哈希表） | 窗口滑动时 **O(len) 重新判回文**，信息完全丢弃重来 |
| 整体 \(O(n)\) 或 \(O(n \cdot \Sigma)\) | 整体最坏 \(O(n^3)\) |

**面试答法**：先讲「for 枚举长度 + 窗口滑动」的直觉，然后主动指出它的复杂度局限 → 自然过渡到「怎么优化判回文？」→ 引出中心扩展（从中心扩天然避免重复扫）或 DP（用子结构复用）——**能自我批判、再抛出更优解是加分信号**。

### 8. 相关题与 Manacher

| 题目 | 关系 |
|:---|:---|
| [647. 回文子串](647.%20回文子串.md) | 同样中心扩展，但目标是**统计个数**——每扩成功一次计 1，模板几乎复用 |
| 516. 最长回文子序列 | 不连续版本，区间 DP |
| 125. 验证回文串 / [234. 回文链表](234.%20回文链表.md) | 判回文基础：双指针对撞 / 反转后半段 |
| Manacher 算法 | \(O(n)\)：插入 `#` 统一奇偶 + 利用已算回文的镜像信息跳过重复扩展，**面试加分项，非必需** |

> 进阶套路归档见 [算法思想/字符串.md](../算法思想/字符串.md) 回文系一节。

> 记忆：**最长回文子串 = 中心扩展：2n−1 个中心（字符 + 间隙）向外扩，记「起点 + 长度」；递归→记忆化→DP 一条链：isPal = 首尾相等 && 内部回文，DP 版 i 必须倒序。**
