doc



#### EasyBI



### 性能优化

#### 你是如何发现接口的优化点

1. 利用arthas 生成火焰图
2. 查看线程调用栈

#### 排查cpu 飙升

1. 使用top命令查询cpu比较高的进程的id号
2. 使用top -Hp id  查看线程占用cpu高的线程id
3. 线程id转16进制
4. jstack 查看线程的执行堆栈

#### 排查内存泄漏

- -XX:+HeapDumpOnOutOfMemoryError参数
- Eclipse Memory Analyzer (MAT)
- jmap



#### 内存溢出



#### 常见的排查命令

##### jstack



### 操作系统

#### 操作系统的多级缓存

#### 多级缓存如何保证数据一致性

缓存一直性是由于引入缓存而导致的问题，所以为了解决缓存不一致的问题，人们提供了很多方案。

1. 通过在总线上加lock锁的方式
2. 通过缓存一直性协议



##### 通过在总线上加lock锁的方式

锁住了总线导致cpu无法访问内存，会导致效率低下。因此出现了第二种解决方案，通过缓存一直性问题来解决一直性的问题。



##### 通过缓存一致性协议

缓存一直性协议，保证了每个缓存中使用的共享变量副本是一致的。

**核心思想**

MESI协议中，每个缓存有四个状态，它们分别是

- M(Modified): 这行数据有效，数据被修改了，和内存中的数据不一致，数据只存于Cache中
- E（Exclusive): 这行数据有效，数据和内存中的数据一致，数据只存在本Cache中
- S(Shared): 这行数据有效，数据和内存中的数据一致，数据存在云很多cache中
- I(invalid): 这行数据无效。

当一个处理器修改了一个缓存行时，他会将状态设置为Modified，并通知其他处理器或核心该缓存已被修改，从而导致其他处理器的响应的缓存行变为invalid。



#### 进程和线程的区别

##### 进程

- 进程是操作系统资源分配的基本单位
- 进程具有一个独立的执行环境。进程是程序在计算机上的执行活动。
- 进程的通信方式有三种
  - 共享内存
  - 消息传递
  - 共享文件



##### 线程

- 线程是cpu分配的基本单位

- 线程分类

  - 用户线程

  - 守护线程

    用户线程一般执行用户的任务，守护线程执行后台任务，守护线程最典型的应用就是GC 垃圾回收器。

- 线程有时也被称为轻量级进程。进程和线程提供了一个执行环境，但创建一个新的线程比创建一个新的进程需要的资源更少。线程是在进程中存在的，每个进程必须有一个线程。线程有如下特点：

  - 线程共享进程的资源，包括内存和打开文件。线程之间通信不用进行系统调用，更节约时间。
  - 线程更轻量级，线程实体包括程序，数据和TCB。
  - 线程是调度的基本单位。

##### jdk19虚线程



##### 区别

1. 定义：
   - 进程：进程是一个独立的执行单元，每个进程都有自己独立的内存空间和系统资源，包括文件描述符、寄存器状态、堆栈等。进程之间通常需要通过进程间通信（IPC）来进行数据交换。
   - 线程：线程是进程内的执行单元，多个线程可以共享同一个进程的内存空间和系统资源。线程之间的通信更加容易，因为它们共享相同的内存地址空间。
2. 资源消耗：
   - 进程：由于每个进程都有独立的资源，创建和销毁进程的开销较大，占用更多的系统资源。
   - 线程：线程共享进程的资源，因此创建和销毁线程的开销较小，并且占用较少的系统资源。
3. 并发性
   - 进程：进程之间的并发性相对较低，因为它们需要通过IPC来进行通信，这会引入一定的开销和复杂性。
   - 线程：线程之间的并发性更高，因为它们可以直接访问共享的内存，无需复杂的通信机制。
4. 安全性
   - 进程：由于进程之间有独立的内存空间，一个进程的错误通常不会影响其他进程，提供了一定的隔离性。
   - 线程：线程之间共享相同的内存空间，一个线程的错误可能会影响到同一进程中的其他线程，因此需要更加小心地管理线程间的共享数据和资源。
5. 创建和销毁：
   - 进程：创建和销毁进程相对较慢，因为需要分配和释放独立的内存空间和资源。
   - 线程：创建和销毁线程相对较快，因为它们共享进程的资源
6. 调度：
   - 进程：进程之间的切换需要更多的时间，因为需要保存和恢复完整的进程上下文。
   - 线程：线程之间的切换更快，因为它们共享相同的进程上下文，只需要保存和恢复少量线程特定的数据。

### 集合

### Spring

#### Spring初始化流程



#### BeanFactory初始化流程



#### ObjectFactory



BeanFactoryPostProcessor

#### Bean

##### bean的种类

- 普通Bean

- 工厂Bean

##### bean的作用域

1. singleton：默认作用域Spring IOC容器仅存在一个Bean实例，Bean以单例方式存在，在创建容器时就同时自动创建一个Bean对象。作用域范围ApplicationContext中。
2. prototype：每次从容器中调用Bean，都会返回新的实例，每次调用getBean。作用域返回时getBean调用至方法结束
3. request： 每次Http请求都会创建一个新的Bean，作用域范围时每次发起Http请求直至拿到对应的结果。该作用域是WebApplicationContext
4. session： 首次http请求创建一个实例，作用域是浏览器首次访问直至浏览器关闭。

​		同一个HTTP Session共享Bean，不同Session使用不同的Bean，仅适用于WebApplicationContext 环境。

5. global-session：作用域范围是WebApplicationContext中。一般用于Portlet应用环境，该运用域仅适用于WebApplicationContext环境。

**范围比较**

prototype < request < session < global-session < singleton

#### bean的生命周期

1. 

#### bean的生命周期提供了那些接口

FactoryBeanPostProcessor

BeanPostProcessor

##### InitializingBean

1. InitializeingBean接口为Bean提供了初始化方法的方式，它只包括afterPropertiesSet方法，然后再调用init-method

#### bean的初始化修改，有哪些方式

1. FactoryBean

spring

#### Bean注入的方式

1. xml
   1. Bean的Set方法注入
   2. 构造器注入
   3. 静态工厂注入
   4. 实例工厂注入
2. @Bean注解
3. @Import

@Import注解也可以用来将Bean注入到Spring容器中，@Import注解导入的类可以分为三种情况：

- 普通类
- 类实现了ImportSelector接口
- 类实现了ImportBeanDefinitionRegistrar接口

4. UserImportBeanDefinitionRegistrar

```java
public class UserImportBeanDefinitionRegistrar implements ImportBeanDefinitionRegistrar {

    @Override
    public void registerBeanDefinitions(AnnotationMetadata importingClassMetadata, BeanDefinitionRegistry registry, BeanNameGenerator importBeanNameGenerator) {
        //构建一个 BeanDefinition , Bean的类型为 User
        AbstractBeanDefinition beanDefinition = BeanDefinitionBuilder.rootBeanDefinition(User.class)
                //设置User这个Bean的属性username的值为三友的java日记
                .addPropertyValue("username", "三友的java日记")
                .getBeanDefinition();
        //把User的BeanDefinition注入到BeanDefinitionRegistry中
        registry.registerBeanDefinition("user", beanDefinition);
    }

}
```

```java
@Import(UserImportBeanDefinitionRegistrar.class)
public class UserImportBeanDefinitionRegistrarDemo {

    public static void main(String[] args) {
        AnnotationConfigApplicationContext applicationContext = new AnnotationConfigApplicationContext();
        applicationContext.register(UserImportBeanDefinitionRegistrarDemo.class);
        applicationContext.refresh();
        User user = applicationContext.getBean(User.class);
        System.out.println(user);
    }

}
```



4. BeanFactoryPostProcessor

RegisterUserBeanFactoryPostProcessor实现BeanFactoryPostProcessor， 往Spring容器中添加一个手动创建的User对象

```java
public class RegisterUserBeanFactoryPostProcessor implements BeanFactoryPostProcessor {

    @Override
    public void postProcessBeanFactory(ConfigurableListableBeanFactory beanFactory) throws BeansException {
        //创建一个User对象
        User user = new User();
        user.setUsername("三友的java日记");
        //将这个User对象注入到Spring容器中
        beanFactory.registerSingleton("user", user);
    }

}
```

4. 通过FactoryBean

FactoryBean这中注入的方式使用也是非常多的，就拿上面举例的OpenFeign来说，OpenFeign为每个FeignClient的接口创建的BeanDefinition的Bean的class类型FeignClientFactoryBean就是FactoryBean的实现。

```java
class FeignClientFactoryBean implements FactoryBean<Object>, InitializingBean, ApplicationContextAware {
    
    // FeignClient接口类型
    private Class<?> type;
    
    @Override
    public Object getObject() throws Exception {
       return getTarget();
    }
    
    @Override
    public Class<?> getObjectType() {
       return type;
    }
}
```



#### AOP





#### 





#### 循环依赖



#### 事务



##### **传播级别**

- required 如果有事务就加入事务，如果没事务就开启一个事务。
- required_new 每次开一个新的事务
- suported 有事务就加入事务，没事务就普通执行
- not_suppoted 有事务就加入事务，没有事务就普通执行
- mandatory 强制有事务，没事务就报警
- never 不支持事务，有事务就报警
- nested， 如果之前有事务，则创建嵌套事务，嵌套事务不影响父事务，反之父事务影响嵌套事务。



##### 失效

1. 动态代理失效
   1. this调用
   2. @Transaction 修饰私有方法
2. 错误的使用隔离级别 
   1. not_support
3. rollbackFor设置错误
4. 异常被catch
5. 数据库引擎不支持事务



**技巧**

1. 大事务放在后面执行，小事务放在前面，降低事务回滚的成本。



#### 声明式事务

##### 源码分析





#### 编程事事务



#### 半消息事务分布式事务





#### 分布式事务



### RocketMq

#### 保持幂等



#### 保持消息顺序性消费





### Redis



#### 集群模式



### juc



#### volatile 

- 缓存一直性协议
- 禁止指令重排序

程序在执行过程中会在执行过程中对指令进行优化，并不会按照我们写的代码先后顺序执行。

volatile 通过内存屏障来禁止指令重排序，这就保证了程序会按照代码的先后顺序执行。这就保证了有序性。被volatile关键字修饰的变量，会严格按照代码顺序执行，load->add->save的执行顺序。

如经典的double check ，就是因为volatile加了内存屏障。



#### AQS



#### 死锁

##### 必要条件

1. 互斥
2. 请求保持
3. 不可剥夺
4. 循环等待

##### 破坏死锁

1. 避免互斥
2. 请求超时， try lock
3. 响应中断， lock interrupt
4. 资源顺序表，有序加锁。



##### 







#### 锁分类



##### 悲观锁 和乐观锁



##### 独占锁和共享锁



##### 公平锁和非公平锁



##### synchronize



#### cas

##### 实现机制



##### ABA



#### 



#### ThreadLocal



#### 线程池

##### 工作流程



##### 应用场景



##### 参数设计

1. cpu 密集型和io密集型

##### 如何监控

1. threadFactory 取名
2. 并发编程的优化



#### 创建线程的几种方式

- 继承Thread类重写run方法
- 实现Runnable方法，调用Thread构造方法
- 通过Callable和FutureTask创建线程
- 通过线程池创建
  - completablefuture
  - 线程池

### jvm

#### 类加载过程

启动类加载器

平台类加载器

应用加载器





线程上下文加载器





#### 内存区域

##### 1.7



##### 1.8





#### 判断是否是垃圾

##### 引用计数



##### 可达性分析



##### 三色标记







#### Mysql

#### 优化手段

##### 查询优化

1. 使用索引

   1. 删除冗余索引
   2. 适配最左前缀原则，避免失效
   3. 使用覆盖索引
   4. 索引下推

2. 避免使用select * from mysql

3. 优化深分页查询

   1. 延迟链接
   2. 记录上次分页的id，前端回传

4. 数据库连接不够

   1. 分库
   2. 读写分离

5. 数据量大

   1. 水平分表
   2. 垂直拆表

6. 慢查询日志

   1. 怎么配置

   

#### explain 关键字

##### 关键字段





#### 索引失效的场景





#### 四种引用

- 强引用
- 弱引用
- 软引用
- 虚引用





### Mybatis

#### SpringBoot启动时候spring 源码



#### 源码



##### MetaObject







#### ErrorContext

errorContext 打印的效果

![image-20230917113224859](../../../Library/Application Support/typora-user-images/image-20230917113224859.png)

告诉你的执行错误大概在哪里。



#### sql 封装









#### sql 注入



#### 一级二级缓存

### SpringBoot

#### run方法执行哪些步骤

#### 哪些注解，各自的作用是什么





